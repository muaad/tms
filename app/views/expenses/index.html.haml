%h1 Listing expenses

%table
  %thead
    %tr
      %th Expense category
      %th Amount
      %th Product
      %th Driver
      %th Truck
      %th Turn boy
      %th User
      %th Description
      %th Lpo
      %th
      %th
      %th

  %tbody
    - @expenses.each do |expense|
      %tr
        %td= expense.expense_category
        %td= expense.amount
        %td= expense.product
        %td= expense.driver
        %td= expense.truck
        %td= expense.turn_boy
        %td= expense.user
        %td= expense.description
        %td= expense.lpo
        %td= link_to 'Show', expense
        %td= link_to 'Edit', edit_expense_path(expense)
        %td= link_to 'Destroy', expense, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Expense', new_expense_path
