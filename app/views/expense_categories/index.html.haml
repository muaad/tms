%h1 Listing expense_categories

%table
  %thead
    %tr
      %th Name
      %th
      %th
      %th

  %tbody
    - @expense_categories.each do |expense_category|
      %tr
        %td= expense_category.name
        %td= link_to 'Show', expense_category
        %td= link_to 'Edit', edit_expense_category_path(expense_category)
        %td= link_to 'Destroy', expense_category, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Expense category', new_expense_category_path
